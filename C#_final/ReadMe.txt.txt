Создали и инициализировали два массива
Первый исходный массив строк
Второй массив задан для формирования значений из первого массива при выполнении условия

Создали метод Arrays
Создали цикл for
Вложили в цикл условие ,если элемент массива меньше или равен 3 знакам,
если условие выполняется элемент с таким то индексом добавляется в новый массив,если же нет,
то уходим на следующую итерацию и т.д.

После мы создаем вторую функцию,которая будет нам выводить сформированный массив из значений первого.

в конце мы вызываем эти функции,передавая в них соотвествтующие параметры.